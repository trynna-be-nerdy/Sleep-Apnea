import { View, Text } from "react-native";

export default function AuthPermission() {
  return (
    <View style={{ flex: 1, alignItems: "center", justifyContent: "center", padding: 20 }}>
      <Text>Auth permission screen placeholder</Text>
    </View>
  );
}
