import { View, Text } from "react-native";

export default function AuthPreferences() {
  return (
    <View style={{ flex: 1, alignItems: "center", justifyContent: "center", padding: 20 }}>
      <Text>Auth preferences placeholder</Text>
    </View>
  );
}
